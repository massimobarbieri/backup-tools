# What should I backup?
# Usato in tutti gli stript di backup
SORGENTE="/home"

# Backup type
#TYPE="media"
 
# Where I have to put backup?
# Usato solo se la destinazione è montata sul client
TARGET="/media/backup"

# Where is the exclude file?
# Usato in tutti gli stript di backup
EXCLUDE="/home/max/.rsync/exclude"

# Which tag should I use for log?
# Usato in tutti gli script di backup
TAG="backup-tools"

